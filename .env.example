# MongoDB Atlas connection string (Server-only — never prefixed with NEXT_PUBLIC_)
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority
# Database name (collection used by this app is "wedding_vendors")
MONGODB_DB=good-things-tracker
